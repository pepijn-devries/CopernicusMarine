% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cms_ncdf.R
\name{cms_write_ncdf}
\alias{cms_write_ncdf}
\title{Store Typical Copernicus Marine Rasters Objects as NCDF}
\usage{
cms_write_ncdf(x, file, missval = -999, prec = "double", ...)
}
\arguments{
\item{x}{A \code{stars} class object created with \code{\link[=cms_download_subset]{cms_download_subset()}}.
Note that this is not a highly generic NetCDF writer, so it
makes certain assumptions about the \code{stars} object, that
may not be true for all.}

\item{file}{File path where to store the object.}

\item{missval}{Value used to represent missing data (\code{NA})
in your object.}

\item{prec}{Precision used for storing the data (\code{"double"}
by default).}

\item{...}{Ignored.}
}
\value{
Returns \code{NULL} invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}} The \code{\link[=cms_download_subset]{cms_download_subset()}} returns a \code{stars} class object.
This is fine if you want to use it directly in R. But if
you want to open it in external software, you need a more
acceptable exchange format. You can use this function to
store the \code{stars} object as a \href{https://en.wikipedia.org/wiki/NetCDF}{NetCDF}
file.
}
\details{
Note that dimensions returned by \code{\link[=cms_download_subset]{cms_download_subset()}}
have interval values, describing between what range the
raster cell lies. When reading a file stored with
\code{cms_write_ncdf()}, you must specify where from the file
the bounds of the dimension should be read. Otherwise
the GDAL driver will (often incorrectly) deduce dimension
values. The example shows how to read the written file
using \code{\link[stars:read_mdim]{stars::read_mdim()}}.

The \code{cms_write_ncdf()} function is tailored to \code{stars}
objects returned by \code{cms_download_subset()}. It might
work on other \code{stars} objects, but it is not guaranteed.

Writing multidimensional data to a standardised format is the
source of many headaches. The current implementation is not ideal,
which is why it is currently experimental. Perhaps future GDAL
release will have better support for writing higher dimension
raster data. To be safe, you can also save your data as \code{.rdata},
or reduce the dimensions by storing specific slices.
}
\examples{
if (interactive()) {
  ## Download some data to store
  mydata <- cms_download_subset(
    product       = "GLOBAL_ANALYSISFORECAST_PHY_001_024",
    layer         = "cmems_mod_glo_phy-cur_anfc_0.083deg_P1D-m",
    variable      = c("uo", "vo"),
    region        = c(-1, 50, 10, 55),
    timerange     = c("2025-01-01 UTC", "2025-01-02 UTC"),
    verticalrange = c(0, -2)
  )
  fn <- tempfile(fileext = ".nc")
  cms_write_ncdf(mydata, fn)
  mydata2 <- stars::read_mdim(
    fn,
    ## You explicitly need to specify dimension bounds when reading back the data
    bounds = c(
      longitude = "longitude_bnds",
      latitude  = "latitude_bnds",
      elevation = "elevation_bnds"))

  ## clean up after our selves
  unlink(fn, TRUE, TRUE)
}
}
\seealso{
Other supporting:
\code{\link[=cms_get_client_info]{cms_get_client_info()}},
\code{\link[=cms_glossary]{cms_glossary()}},
\code{\link[=cms_translate]{cms_translate()}}
}
\concept{supporting}
